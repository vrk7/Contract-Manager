[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",   # line too long — handled by formatter
]
per-file-ignores = { "tests/*" = ["E402", "I001"] }

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
